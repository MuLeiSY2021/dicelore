# 谓词引擎扩展 Implementation Plan（Plan B）

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development。Steps 用 checkbox。

**Goal:** 扩 watcher 谓词引擎(spec §3):① `{ns:has(col OP val,...)}` **存在性匹配**(state/plotline/front/foreshadow/log/anchor 全表,返回 bool);② 重算点扩到 **log append 后**(不止 state_update);③ repeat × log 单调匹配的 **since 游标**。值引用保持 state 数值(状态/离散判定改走 has)。

**Architecture:** has() 不走 evalExpr 数值路径——在 `predicate.ts` 入口加分支,检测 `{ns:has(...)}` 后交 `ctx.existsMatch`(新 `store/existsMatch.ts` 的 `makeExistsMatch(db)` 提供,白名单列拼 `SELECT 1 ... LIMIT 1`)。`EvalCtx` 加可选 `existsMatch`。重算点:在 `mcp/handlers/event.ts` 的 appendHandler 里 logAppend 后调 `recomputeWatchers`;since 游标由 recompute 给每个 repeat watcher 的 ctx 注入 `last_fired_seq`。

**Tech Stack:** TypeScript、better-sqlite3、Vitest。

## Global Constraints

- `{实体.attr}` 旧值引用约定不破(无 ns 前缀 = state);新增 `{state:X.attr}` 前缀亦走 state。**非 state 的值引用不支持**(状态用 has)。
- has() 列**白名单**(防注入),值**参数绑定**;未知 ns / 未知列 → throw `BAD_INPUT`。
- 不改 dice/evalExpr 数值语义;只在 predicate 层加 has 分支 + EvalCtx 加字段。
- 每任务 `npm test`(基线 51 文件 222 测试)+ `npm run typecheck` 绿;scoped commit(不 `git add -A`,commit 前 `git checkout -- package-lock.json` 还原);message 末尾 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- worktree `.claude/worktrees/predicate`(已 npm install)。

---

### Task 1: `has()` 存在性匹配引擎

**Files:**
- Create: `packages/core/src/store/existsMatch.ts`
- Modify: `packages/core/src/expr/evaluate.ts`（`EvalCtx` 加 `existsMatch?`）
- Modify: `packages/core/src/expr/predicate.ts`（has 分支 + 解析 inner conds）
- Test: `packages/core/src/store/existsMatch.test.ts`、`packages/core/src/expr/predicate.test.ts`（补 has 用例）

**Interfaces:**
- Produces：
  - `interface HasCond { col: string; op: string; val: string }`（op ∈ `=,!=,<,<=,>,>=`）
  - `makeExistsMatch(db): (ns: string, conds: HasCond[], sinceSeq?: number) => boolean`
  - `EvalCtx.existsMatch?: (ns: string, conds: HasCond[], sinceSeq?: number) => boolean`
  - `parseHasConds(inner: string): HasCond[]`（导出供测）

- [ ] **Step 1: 写失败测试**

`existsMatch.test.ts`：

```ts
import { beforeEach, expect, test } from "vitest";
import { initSchema, openDb, type DB } from "./db.js";
import { stateSet } from "./state.js";
import { plotlineUpsert } from "./plotline.js";
import { logAppend } from "./log.js";
import { makeExistsMatch } from "./existsMatch.js";

let db: DB, ex: ReturnType<typeof makeExistsMatch>;
beforeEach(() => { db = openDb(":memory:"); initSchema(db); ex = makeExistsMatch(db); });

test("state 行存在性匹配(列 + 数值比较)", () => {
  stateSet(db, "老大", "敌意", "9", { kind: "npc" } as any);  // 若 stateSet 不收 kind,用 frontUpsert 同款:见下注
  expect(ex("state", [{ col: "attr", op: "=", val: "敌意" }, { col: "value", op: ">=", val: "8" }])).toBe(true);
  expect(ex("state", [{ col: "attr", op: "=", val: "敌意" }, { col: "value", op: ">=", val: "20" }])).toBe(false);
});
test("plotline status 匹配", () => {
  plotlineUpsert(db, { id: "荒漠", title: "荒漠诅咒", status: "open" });
  expect(ex("plotline", [{ col: "id", op: "=", val: "荒漠" }, { col: "status", op: "=", val: "open" }])).toBe(true);
});
test("log 匹配 + sinceSeq 只认更新事件", () => {
  const s1 = logAppend(db, { kind: "choice", content: "驰援" });
  expect(ex("log", [{ col: "kind", op: "=", val: "choice" }])).toBe(true);
  expect(ex("log", [{ col: "kind", op: "=", val: "choice" }], s1)).toBe(false); // seq>s1 无
});
test("未知 ns / 未知列 抛 BAD_INPUT", () => {
  expect(() => ex("evil", [{ col: "x", op: "=", val: "1" }])).toThrow();
  expect(() => ex("state", [{ col: "drop", op: "=", val: "1" }])).toThrow();
});
```

`predicate.test.ts` 补：

```ts
test("{ns:has(...)} 走 existsMatch、不被内部 OP 切开", () => {
  const ctx = { getRef: () => undefined, existsMatch: (ns, conds) => ns === "state" && conds.length === 2 };
  expect(evalPredicate("{state:has(attr=敌意, value>=8)}", ctx as any)).toBe(true);
});
test("无 existsMatch 时 has 谓词抛错", () => {
  expect(() => evalPredicate("{state:has(x=1)}", { getRef: () => undefined } as any)).toThrow();
});
test("普通比较谓词仍走旧路径", () => {
  const ctx = { getRef: (e, a) => (e === "张三" && a === "HP" ? "20" : undefined) };
  expect(evalPredicate("{张三.HP} < 30", ctx as any)).toBe(true);
});
```

> 注：`stateSet` 的签名以实际为准(Plan A 后可能是 `stateSet(db, entity, attr, value, visible?)`;kind 走列默认)。测试里要造一行 `kind=npc` 的 state,用实际可行的写法(若 stateSet 不直接收 kind,可直接 `db.prepare("INSERT INTO state(entity,attr,value,kind) VALUES(?,?,?, 'npc')").run(...)` 造数据)。实现时核对 `state.ts` 签名。

- [ ] **Step 2: 跑确认失败**

Run: `cd packages/core && npx vitest run src/store/existsMatch.test.ts src/expr/predicate.test.ts`
Expected: FAIL（模块/分支不存在）

- [ ] **Step 3: 实现**

`existsMatch.ts`：

```ts
import type { DB } from "./db.js";
import { DiceloreError } from "../errors.js";

export interface HasCond { col: string; op: string; val: string; }

const TABLES: Record<string, { table: string; cols: Set<string> }> = {
  state: { table: "state", cols: new Set(["entity","attr","value","kind","visible","rel_object","rel_dim","clock_min","clock_max","clock_mode"]) },
  plotline: { table: "plotline", cols: new Set(["id","title","summary","status"]) },
  front: { table: "front", cols: new Set(["id","name","stakes","clock_ref","status"]) },
  foreshadow: { table: "foreshadow", cols: new Set(["id","content","status"]) },
  log: { table: "log", cols: new Set(["seq","kind","content","tags","is_moment","visible"]) },
  anchor: { table: "anchor", cols: new Set(["owner_table","owner_id","target_table","target_id","role"]) },
};
const SQL_OP: Record<string, string> = { "=":"=", "!=":"!=", "<":"<", "<=":"<=", ">":">", ">=":">=" };

export function makeExistsMatch(db: DB) {
  return (ns: string, conds: HasCond[], sinceSeq?: number): boolean => {
    const def = TABLES[ns];
    if (!def) throw new DiceloreError("BAD_INPUT", `has(): 未知命名空间 "${ns}"`);
    const wh: string[] = []; const params: unknown[] = [];
    for (const c of conds) {
      if (!def.cols.has(c.col)) throw new DiceloreError("BAD_INPUT", `has(): ${ns} 无列 "${c.col}"`);
      const sop = SQL_OP[c.op];
      if (!sop) throw new DiceloreError("BAD_INPUT", `has(): 非法算符 "${c.op}"`);
      const n = Number(c.val);
      wh.push(`${c.col} ${sop} ?`);
      params.push(Number.isFinite(n) && /^-?\d+(\.\d+)?$/.test(c.val.trim()) ? n : c.val);
    }
    if (ns === "log" && sinceSeq !== undefined) { wh.push("seq > ?"); params.push(sinceSeq); }
    const sql = `SELECT 1 FROM ${def.table}${wh.length ? " WHERE " + wh.join(" AND ") : ""} LIMIT 1`;
    return db.prepare(sql).get(...params) !== undefined;
  };
}
```

`evaluate.ts`：`EvalCtx` 加 `existsMatch?: (ns: string, conds: import("../store/existsMatch.js").HasCond[], sinceSeq?: number) => boolean;`（或把 HasCond 移到无环依赖处；若 store→expr 反向依赖有环,把 `HasCond` 定义放 `expr/predicate.ts` 并由 existsMatch.ts import）。**实现时择无环方案**:推荐 `HasCond` 定义在 `expr/predicate.ts`,`existsMatch.ts` 从 predicate import 类型。

`predicate.ts`：顶部加 has 分支 + 解析：

```ts
export interface HasCond { col: string; op: string; val: string; }
const HAS_RE = /^\{(\w+):has\((.+)\)\}$/;
const HAS_OPS = ["<=",">=","!=","<",">","="]; // = 用于 has 内等值

export function parseHasConds(inner: string): HasCond[] {
  return inner.split(",").map((seg) => {
    const s = seg.trim();
    for (const op of HAS_OPS) {
      const at = s.indexOf(op);
      if (at !== -1) return { col: s.slice(0, at).trim(), op, val: s.slice(at + op.length).trim().replace(/^['"]|['"]$/g, "") };
    }
    throw new Error(`has(): 段缺算符 "${s}"`);
  });
}

export function evalPredicate(pred: string, ctx: EvalCtx): boolean {
  const t = pred.trim();
  const hm = t.match(HAS_RE);
  if (hm) {
    if (!ctx.existsMatch) throw new Error(`evalPredicate: has() 需 existsMatch — "${pred}"`);
    return ctx.existsMatch(hm[1], parseHasConds(hm[2]));
  }
  // ...原比较逻辑不变...
}
```

> `=` 放 HAS_OPS 末尾(`<=`/`>=`/`!=` 长算符优先,避免把 `>=` 切成 `>`+`=`)。

- [ ] **Step 4: 跑确认通过 + 全套**

Run: `cd packages/core && npm test && npm run typecheck`
Expected: 全绿。

- [ ] **Step 5: Commit**（scoped: existsMatch.ts/.test.ts + evaluate.ts + predicate.ts/.test.ts）

---

### Task 2: makeEvalCtx —— 统一 ctx 工厂(getRef + existsMatch),接进 mutate

**Files:**
- Create: `packages/core/src/store/evalCtx.ts`
- Modify: `packages/core/src/store/mutate.ts`（用 `makeEvalCtx` 替内联 getRef）
- Test: `packages/core/src/store/evalCtx.test.ts`

**Interfaces:**
- Consumes：`makeExistsMatch`（Task 1）、`stateGet`。
- Produces：`makeEvalCtx(db, opts?: { rng?: Rng }): EvalCtx`——getRef 支持 `{X.attr}` 与 `{state:X.attr}`(均 state);existsMatch = makeExistsMatch(db)。

- [ ] **Step 1: 写失败测试**

```ts
test("getRef: 裸名 与 state: 前缀都走 state", () => {
  const db = openDb(":memory:"); initSchema(db);
  stateSet(db, "张三", "HP", "20");
  const ctx = makeEvalCtx(db);
  expect(ctx.getRef("张三", "HP")).toBe("20");
  expect(ctx.getRef("state:张三", "HP")).toBe("20");
});
test("ctx 带 existsMatch", () => {
  const db = openDb(":memory:"); initSchema(db);
  plotlineUpsert(db, { id: "p", title: "t", status: "open" });
  const ctx = makeEvalCtx(db);
  expect(ctx.existsMatch!("plotline", [{ col: "status", op: "=", val: "open" }])).toBe(true);
});
```

- [ ] **Step 2: 跑确认失败** → **Step 3: 实现**

`evalCtx.ts`：

```ts
import type { DB } from "./db.js";
import type { EvalCtx, RefGetter } from "../expr/evaluate.js";
import type { Rng } from "../dice/index.js";
import { stateGet } from "./state.js";
import { makeExistsMatch } from "./existsMatch.js";

export function makeEvalCtx(db: DB, opts?: { rng?: Rng }): EvalCtx {
  const getRef: RefGetter = (e, a) => {
    const key = e.startsWith("state:") ? e.slice(6) : e;
    return stateGet(db, key, a)?.value;
  };
  return { rng: opts?.rng, getRef, existsMatch: makeExistsMatch(db) };
}
```

`mutate.ts`：把第 60-61 行 `const getRef ...; const ctx = {...}` 换为 `const ctx = makeEvalCtx(db, { rng: opts?.rng });`（import makeEvalCtx）。

- [ ] **Step 4: 全套绿** → **Step 5: Commit**

---

### Task 3: 重算点扩到 log append + repeat×log 的 since 游标

**Files:**
- Modify: `packages/core/src/store/watcher.ts`（`recomputeWatchers` 给每个 repeat watcher 注入 `last_fired_seq` since）
- Modify: `packages/core/src/mcp/handlers/event.ts`（appendHandler 在 logAppend 后调 `recomputeWatchers`）
- Test: `packages/core/src/store/watcher.test.ts`（补 has/log 触发 + since re-arm）、`packages/core/src/mcp/handlers/event.test.ts`（event_append 后触发 log-has watcher）

**Interfaces:**
- Consumes：`makeEvalCtx`（Task 2）、`makeExistsMatch`。
- Produces：`recomputeWatchers` 对 log-has 谓词按 watcher 注入 since;appendHandler 写完触发重算并把 fired 并入出参。

- [ ] **Step 1: 写失败测试**

```ts
// watcher.test.ts
test("log-has watcher: once 出现即触发一次", () => {
  const db = openDb(":memory:"); initSchema(db);
  watcherSet(db, { condition: "{log:has(kind=choice)}", payload: "选了", mode: "once" });
  logAppend(db, { kind: "choice", content: "驰援" });
  const fired = recomputeWatchers(db, makeEvalCtx(db));
  expect(fired.map((f) => f.payload)).toEqual(["选了"]);
  // 再 recompute 不重复(once 已 disarm)
  expect(recomputeWatchers(db, makeEvalCtx(db))).toEqual([]);
});
test("repeat log-has: since 游标只认新事件、可重触发", () => {
  const db = openDb(":memory:"); initSchema(db);
  watcherSet(db, { condition: "{log:has(kind=reveal)}", payload: "揭示", mode: "repeat" });
  logAppend(db, { kind: "reveal", content: "A" });
  expect(recomputeWatchers(db, makeEvalCtx(db)).length).toBe(1); // 触发1
  expect(recomputeWatchers(db, makeEvalCtx(db)).length).toBe(0); // 无新 reveal,re-arm 不触发
  logAppend(db, { kind: "reveal", content: "B" });
  expect(recomputeWatchers(db, makeEvalCtx(db)).length).toBe(1); // 新 reveal,再触发
});
```

`event.test.ts`：`event_append` 一条 `kind=reveal` 后,已设的 log-has watcher 进入 fired(出参或副作用)。

- [ ] **Step 2: 跑确认失败** → **Step 3: 实现**

`watcher.ts` `recomputeWatchers`：对每个 watcher 构造 per-watcher ctx——若 repeat 且已 fire 过,existsMatch 注入 since=last_fired_seq:

```ts
import { makeExistsMatch } from "./existsMatch.js";
export function recomputeWatchers(db: DB, ctx: EvalCtx): { id: number; payload: string }[] {
  const baseExists = ctx.existsMatch ?? makeExistsMatch(db);
  const fired = [];
  for (const w of watcherList(db)) {
    const since = w.mode === "repeat" && w.last_fired_seq != null ? w.last_fired_seq : undefined;
    const wctx: EvalCtx = { ...ctx, existsMatch: (ns, conds) => baseExists(ns, conds, since) };
    const cond = evalPredicate(w.condition, wctx);
    // ...原 armed/fire/re-arm 逻辑不变(用 cond)...
  }
  return fired;
}
```
> `WatcherRow` 需含 `last_fired_seq`——若 `watcherList` 未 SELECT 它,补上(SELECT 加 `last_fired_seq`,interface 加 `last_fired_seq: number|null`)。

`mcp/handlers/event.ts` appendHandler：`logAppend` 后加 `const fired = recomputeWatchers(db, makeEvalCtx(db));`,把 fired 并进出参(参照 sheet_update 出参的 `fired_watchers` 形态)。

- [ ] **Step 4: 全套绿** → **Step 5: Commit**

---

## Self-Review
- spec §3.1 has() 全表存在性匹配 ✓(Task 1);§3.2 重算点扩 log append ✓(Task 3);§3.3 once/repeat×单调 since ✓(Task 3)。
- 值引用：state 数值保持;状态/离散走 has()（spec §3.1 的 `{plotline:X.status}` 值引用**改为 has() 表达**,因数值谓词不做字符串比较——记 §11 偏离,实现时如此)。
- 白名单防注入 ✓(Task 1 TABLES.cols)。
- 类型一致：`HasCond` 定义在 predicate.ts、existsMatch.ts import;`EvalCtx.existsMatch` Task 1 加、Task 2/3 用;`last_fired_seq` Task 3 补进 WatcherRow。
- 偏离记录：状态判定从值引用改 has()（见上）;非 state 值引用不支持(YAGNI)。
