# 叙事/记忆层存储基座 Implementation Plan（Plan A）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement task-by-task. Steps use checkbox (`- [ ]`).

**Goal:** 建叙事/记忆层 5 张物理表（front/plotline/foreshadow/history/anchor）+ watcher 加 `source` 列 + 各表底层 CRUD + `front_omen`/`tension_board` 两张聚合视图（TS 查询函数）。交付问题总账 A2/A3/A5 的**存储基座 + 未结张力看板**。

**Architecture:** 照既有 `store/rule.ts` 模式建 store 模块（interface + 内联 prepared SQL CRUD）；表 DDL 加进 `store/db.ts` 的 `initSchema` 模板串；视图是 TS 查询函数（项目无 SQLite VIEW 先例），`front_omen` 落 `store/front.ts`、`tension_board` 落 `present/tensionBoard.ts`（照 `present/model.ts` 跨表查询风格）。

**Tech Stack:** TypeScript（ESM，`.js` 导入后缀）、better-sqlite3、Vitest、`tsc --noEmit`。

## Global Constraints

- **不改 `src/expr/*`**（谓词引擎扩展是 Plan B，不在本 plan）。
- **不加业务工具**（front_advance 等 → step③ 声明）；本 plan 只到**底层 CRUD + 视图查询函数**。MCP 工具面本 plan 仅给 `watcher_list` 出参补 `source` 字段（小增量），不加新工具。
- **叙事表 v1 GM 全见**（DN-6）：不加 visible 列、不加可见性原语。
- 表/列命名严格按 spec §4：`front(id,name,stakes,clock_ref,status)` / `plotline(id,title,summary,status)` / `foreshadow(id,content,status)` / `history(id,seq_from,seq_to,summary,created_seq)` / `anchor(id,owner_table,owner_id,target_table,target_id,role)` / `watcher` 加 `source`。
- 每个任务结束：`npm test`（基线 45 文件 210 测试）+ `npm run typecheck` 全绿；scoped `git add`（**不 `git add -A`**，工作树含 package-lock 等无关改动），message 末尾 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- worktree `.claude/worktrees/narrative`（已 npm install）。

---

### Task 1: schema —— 5 张新表 + watcher.source 列

**Files:**
- Modify: `packages/core/src/store/db.ts`（`initSchema` 的 `db.exec` 模板串末尾加 5 张表 DDL；watcher 表加 `source` 列）
- Modify: `packages/core/src/store/db.test.ts`（表存在性断言数组加新表）
- Modify: `packages/core/src/store/watcher.ts`（`WatcherRow` 加 `source`；`watcherSet` opts 加 `source?`；`watcherList` SELECT 加 `source`）
- Modify: `packages/core/src/store/watcher.test.ts`（加 source 默认/可设断言）

**Interfaces:**
- Produces：表 `front`/`plotline`/`foreshadow`/`history`/`anchor` 建出；`watcher` 多 `source TEXT NOT NULL DEFAULT 'manual'` 列；`watcherSet(db, {condition, payload, mode?, created_seq?, source?})`；`WatcherRow` 含 `source: string`。

- [ ] **Step 1: 写失败测试（db.test.ts 表断言 + watcher source）**

在 `db.test.ts` 现有表名数组里追加 `"front","plotline","foreshadow","history","anchor"`。在 `watcher.test.ts` 加：

```ts
test("watcherSet 默认 source='manual'、可设 source", () => {
  const id1 = watcherSet(db, { condition: "{张三.HP} < 10", payload: "濒死" });
  const id2 = watcherSet(db, { condition: "{张三.HP} < 5", payload: "凶兆", source: "front:魔道入侵" });
  const list = watcherList(db);
  expect(list.find((w) => w.id === id1)!.source).toBe("manual");
  expect(list.find((w) => w.id === id2)!.source).toBe("front:魔道入侵");
});
```

- [ ] **Step 2: 跑确认失败**

Run: `cd packages/core && npx vitest run src/store/db.test.ts src/store/watcher.test.ts`
Expected: FAIL（表不存在 / `source` 字段无）

- [ ] **Step 3: 加表 DDL + watcher.source**

在 `db.ts` 的 `initSchema` `db.exec(\`...\`)` 模板串末尾追加：

```sql
CREATE TABLE IF NOT EXISTS front (
  id TEXT PRIMARY KEY, name TEXT NOT NULL, stakes TEXT,
  clock_ref TEXT, status TEXT NOT NULL DEFAULT 'active'
);
CREATE TABLE IF NOT EXISTS plotline (
  id TEXT PRIMARY KEY, title TEXT NOT NULL, summary TEXT,
  status TEXT NOT NULL DEFAULT 'open'
);
CREATE TABLE IF NOT EXISTS foreshadow (
  id TEXT PRIMARY KEY, content TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'planted'
);
CREATE TABLE IF NOT EXISTS history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  seq_from INTEGER NOT NULL, seq_to INTEGER NOT NULL,
  summary TEXT NOT NULL, created_seq INTEGER NOT NULL
);
CREATE TABLE IF NOT EXISTS anchor (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  owner_table TEXT NOT NULL, owner_id TEXT NOT NULL,
  target_table TEXT NOT NULL, target_id TEXT NOT NULL,
  role TEXT
);
```

并把 watcher 表 DDL 的列加一行 `source TEXT NOT NULL DEFAULT 'manual',`（放 `status` 列附近）。

在 `watcher.ts`：`WatcherRow` interface 加 `source: string`；`watcherSet` 的 opts 类型加 `source?: string`，INSERT 语句加 `source` 列（值 `opts.source ?? 'manual'`）；`watcherList` 的 SELECT 加 `source` 列。

- [ ] **Step 4: 跑确认通过 + 全套**

Run: `cd packages/core && npm test && npm run typecheck`
Expected: 全绿；tsc 干净。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/store/db.ts packages/core/src/store/db.test.ts packages/core/src/store/watcher.ts packages/core/src/store/watcher.test.ts
git commit -m "feat(store): 叙事/记忆层 5 表 schema + watcher.source 列

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: store 模块 front / plotline / foreshadow（CRUD）

**Files:**
- Create: `packages/core/src/store/front.ts`、`plotline.ts`、`foreshadow.ts`
- Test: `packages/core/src/store/front.test.ts`、`plotline.test.ts`、`foreshadow.test.ts`

**Interfaces:**
- Consumes：Task 1 的表。
- Produces：
  - `interface Front { id; name; stakes: string|null; clock_ref: string|null; status: string }`；`frontUpsert(db, {id, name, stakes?, clock_ref?, status?}): void`；`frontGet(db, id): Front|undefined`；`frontList(db): Front[]`；`frontSetStatus(db, id, status): void`
  - `interface Plotline { id; title; summary: string|null; status }`；`plotlineUpsert(db, {id, title, summary?, status?})`；`plotlineGet`/`plotlineList`/`plotlineSetStatus` 同形
  - `interface Foreshadow { id; content; status }`；`foreshadowUpsert(db, {id, content, status?})`；`foreshadowGet`/`foreshadowList`/`foreshadowSetStatus` 同形

- [ ] **Step 1: 写失败测试（三文件，每个一组 CRUD 断言）**

`front.test.ts` 示例（plotline/foreshadow 照此换字段）：

```ts
import { beforeEach, describe, expect, test } from "vitest";
import { initSchema, openDb, type DB } from "./db.js";
import { frontUpsert, frontGet, frontList, frontSetStatus } from "./front.js";

let db: DB;
beforeEach(() => { db = openDb(":memory:"); initSchema(db); });

describe("front store", () => {
  test("upsert 后 get（默认 status=active）", () => {
    frontUpsert(db, { id: "魔道入侵", name: "魔道入侵", stakes: "护山大阵能否建成", clock_ref: "世界.入侵进度" });
    expect(frontGet(db, "魔道入侵")).toMatchObject({
      id: "魔道入侵", name: "魔道入侵", stakes: "护山大阵能否建成", clock_ref: "世界.入侵进度", status: "active",
    });
  });
  test("setStatus 改 spent；list 返回全部", () => {
    frontUpsert(db, { id: "f1", name: "线1" });
    frontSetStatus(db, "f1", "spent");
    expect(frontGet(db, "f1")!.status).toBe("spent");
    expect(frontList(db).map((f) => f.id)).toEqual(["f1"]);
  });
});
```

- [ ] **Step 2: 跑确认失败**

Run: `cd packages/core && npx vitest run src/store/front.test.ts src/store/plotline.test.ts src/store/foreshadow.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现三 store 模块（照 `store/rule.ts` 模式：interface + 内联 prepared SQL）**

`front.ts`：

```ts
import type { DB } from "./db.js";

export interface Front { id: string; name: string; stakes: string | null; clock_ref: string | null; status: string; }

export function frontUpsert(db: DB, f: { id: string; name: string; stakes?: string; clock_ref?: string; status?: string }): void {
  db.prepare(
    `INSERT INTO front(id, name, stakes, clock_ref, status) VALUES(?,?,?,?,COALESCE(?,'active'))
     ON CONFLICT(id) DO UPDATE SET name=excluded.name, stakes=excluded.stakes, clock_ref=excluded.clock_ref, status=excluded.status`
  ).run(f.id, f.name, f.stakes ?? null, f.clock_ref ?? null, f.status ?? null);
}
export function frontGet(db: DB, id: string): Front | undefined {
  return db.prepare(`SELECT id, name, stakes, clock_ref, status FROM front WHERE id = ?`).get(id) as Front | undefined;
}
export function frontList(db: DB): Front[] {
  return db.prepare(`SELECT id, name, stakes, clock_ref, status FROM front ORDER BY id`).all() as Front[];
}
export function frontSetStatus(db: DB, id: string, status: string): void {
  db.prepare(`UPDATE front SET status = ? WHERE id = ?`).run(status, id);
}
```

`plotline.ts`（字段 title/summary/status，默认 'open'）、`foreshadow.ts`（字段 content/status，默认 'planted'）照此结构写。

- [ ] **Step 4: 跑确认通过 + 全套**

Run: `cd packages/core && npm test && npm run typecheck`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/store/front.ts packages/core/src/store/plotline.ts packages/core/src/store/foreshadow.ts packages/core/src/store/front.test.ts packages/core/src/store/plotline.test.ts packages/core/src/store/foreshadow.test.ts
git commit -m "feat(store): front/plotline/foreshadow CRUD

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: store 模块 history / anchor（CRUD + 关系图谱正反查）

**Files:**
- Create: `packages/core/src/store/history.ts`、`anchor.ts`
- Test: `packages/core/src/store/history.test.ts`、`anchor.test.ts`

**Interfaces:**
- Produces：
  - `interface History { id; seq_from; seq_to; summary; created_seq }`；`historyAppend(db, {seq_from, seq_to, summary, created_seq}): number`；`historyList(db): History[]`
  - `interface Anchor { id; owner_table; owner_id; target_table; target_id; role: string|null }`；`anchorAdd(db, {owner_table, owner_id, target_table, target_id, role?}): number`；`anchorsByOwner(db, owner_table, owner_id): Anchor[]`；`anchorsByTarget(db, target_table, target_id): Anchor[]`

- [ ] **Step 1: 写失败测试**

`anchor.test.ts`（关系图谱正反查是 A4 关键）：

```ts
import { beforeEach, describe, expect, test } from "vitest";
import { initSchema, openDb, type DB } from "./db.js";
import { anchorAdd, anchorsByOwner, anchorsByTarget } from "./anchor.js";

let db: DB;
beforeEach(() => { db = openDb(":memory:"); initSchema(db); });

test("正查 owner / 反查 target", () => {
  anchorAdd(db, { owner_table: "plotline", owner_id: "护山之争", target_table: "entity", target_id: "墨大夫", role: "antagonist" });
  anchorAdd(db, { owner_table: "foreshadow", owner_id: "井中影", target_table: "entity", target_id: "墨大夫" });
  expect(anchorsByOwner(db, "plotline", "护山之争").map((a) => a.target_id)).toEqual(["墨大夫"]);
  // 反查：挂在墨大夫上的所有线/伏笔（A4 到点浮现）
  expect(anchorsByTarget(db, "entity", "墨大夫").map((a) => a.owner_id).sort()).toEqual(["井中影", "护山之争"]);
});
```

`history.test.ts`：append 返回自增 id + list 顺序。

- [ ] **Step 2: 跑确认失败**

Run: `cd packages/core && npx vitest run src/store/history.test.ts src/store/anchor.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 history.ts / anchor.ts**

`anchor.ts`：

```ts
import type { DB } from "./db.js";

export interface Anchor { id: number; owner_table: string; owner_id: string; target_table: string; target_id: string; role: string | null; }
const COLS = "id, owner_table, owner_id, target_table, target_id, role";

export function anchorAdd(db: DB, a: { owner_table: string; owner_id: string; target_table: string; target_id: string; role?: string }): number {
  const info = db.prepare(`INSERT INTO anchor(owner_table, owner_id, target_table, target_id, role) VALUES(?,?,?,?,?)`)
    .run(a.owner_table, a.owner_id, a.target_table, a.target_id, a.role ?? null);
  return Number(info.lastInsertRowid);
}
export function anchorsByOwner(db: DB, owner_table: string, owner_id: string): Anchor[] {
  return db.prepare(`SELECT ${COLS} FROM anchor WHERE owner_table=? AND owner_id=? ORDER BY id`).all(owner_table, owner_id) as Anchor[];
}
export function anchorsByTarget(db: DB, target_table: string, target_id: string): Anchor[] {
  return db.prepare(`SELECT ${COLS} FROM anchor WHERE target_table=? AND target_id=? ORDER BY id`).all(target_table, target_id) as Anchor[];
}
```

`history.ts`：`historyAppend` INSERT 返回 `Number(lastInsertRowid)`；`historyList` SELECT ORDER BY id。

- [ ] **Step 4: 跑确认通过 + 全套**

Run: `cd packages/core && npm test && npm run typecheck`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/store/history.ts packages/core/src/store/anchor.ts packages/core/src/store/history.test.ts packages/core/src/store/anchor.test.ts
git commit -m "feat(store): history + anchor(关系图谱)正反查 CRUD

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 视图 front_omen + tension_board（聚合查询函数）

**Files:**
- Modify: `packages/core/src/store/front.ts`（加 `frontOmenList`）
- Create: `packages/core/src/present/tensionBoard.ts`
- Test: `packages/core/src/store/front.test.ts`（补 frontOmenList）、`packages/core/src/present/tensionBoard.test.ts`

**Interfaces:**
- Consumes：Task 1–3 的表 + CRUD；`watcherList`/`WatcherRow`（含 source，Task 1）；`frontList`/`plotlineList`/`foreshadowList`。
- Produces：
  - `frontOmenList(db, frontId): WatcherRow[]`（该 front 的凶兆阶梯 = `source='front:'+frontId` 的 watcher）
  - `tensionBoard(db): { fronts: Front[]; plotlines: Plotline[]; foreshadows: Foreshadow[]; watchers: WatcherRow[] }`（active front + open/active plotline + planted foreshadow + armed watcher）

- [ ] **Step 1: 写失败测试**

`tensionBoard.test.ts`：

```ts
import { beforeEach, describe, expect, test } from "vitest";
import { initSchema, openDb, type DB } from "../store/db.js";
import { frontUpsert, frontSetStatus } from "../store/front.js";
import { plotlineUpsert } from "../store/plotline.js";
import { foreshadowUpsert, foreshadowSetStatus } from "../store/foreshadow.js";
import { watcherSet } from "../store/watcher.js";
import { tensionBoard } from "./tensionBoard.js";

let db: DB;
beforeEach(() => { db = openDb(":memory:"); initSchema(db); });

test("只列未结张力：active front / open|active plotline / planted foreshadow / armed watcher", () => {
  frontUpsert(db, { id: "f1", name: "活线" });
  frontUpsert(db, { id: "f2", name: "已了" }); frontSetStatus(db, "f2", "spent");
  plotlineUpsert(db, { id: "p1", title: "开着的" });               // open
  foreshadowUpsert(db, { id: "fs1", content: "埋着" });            // planted
  foreshadowUpsert(db, { id: "fs2", content: "已收" }); foreshadowSetStatus(db, "fs2", "recalled");
  watcherSet(db, { condition: "{世界.年} > 100", payload: "x" });   // armed

  const b = tensionBoard(db);
  expect(b.fronts.map((f) => f.id)).toEqual(["f1"]);              // 不含 spent f2
  expect(b.plotlines.map((p) => p.id)).toEqual(["p1"]);
  expect(b.foreshadows.map((f) => f.id)).toEqual(["fs1"]);        // 不含 recalled fs2
  expect(b.watchers.length).toBe(1);
});
```

`front.test.ts` 补 `frontOmenList`：

```ts
test("frontOmenList 只返回该 front 的凶兆 watcher", () => {
  frontUpsert(db, { id: "魔道入侵", name: "魔道入侵" });
  watcherSet(db, { condition: "{世界.入侵进度} >= 3", payload: "边境沦陷", source: "front:魔道入侵" });
  watcherSet(db, { condition: "{张三.HP} < 5", payload: "无关", source: "manual" });
  const omens = frontOmenList(db, "魔道入侵");
  expect(omens.map((w) => w.payload)).toEqual(["边境沦陷"]);
});
```

- [ ] **Step 2: 跑确认失败**

Run: `cd packages/core && npx vitest run src/store/front.test.ts src/present/tensionBoard.test.ts`
Expected: FAIL（函数不存在）

- [ ] **Step 3: 实现 frontOmenList + tensionBoard**

`front.ts` 末尾加（导入 `watcherList`/`WatcherRow` from `./watcher.js`）：

```ts
import { watcherList, type WatcherRow } from "./watcher.js";
export function frontOmenList(db: DB, frontId: string): WatcherRow[] {
  return watcherList(db).filter((w) => w.source === `front:${frontId}`);
}
```

`present/tensionBoard.ts`：

```ts
import type { DB } from "../store/db.js";
import { frontList, type Front } from "../store/front.js";
import { plotlineList, type Plotline } from "../store/plotline.js";
import { foreshadowList, type Foreshadow } from "../store/foreshadow.js";
import { watcherList, type WatcherRow } from "../store/watcher.js";

export interface TensionBoard { fronts: Front[]; plotlines: Plotline[]; foreshadows: Foreshadow[]; watchers: WatcherRow[]; }

export function tensionBoard(db: DB): TensionBoard {
  return {
    fronts: frontList(db).filter((f) => f.status === "active"),
    plotlines: plotlineList(db).filter((p) => p.status === "open" || p.status === "active"),
    foreshadows: foreshadowList(db).filter((f) => f.status === "planted"),
    watchers: watcherList(db).filter((w) => w.armed === 1),
  };
}
```

> 注：`watcherList` 的 `armed` 是 0|1 数字（见 watcher.ts）；若其已只返回 active/armed，则 filter 冗余但无害——按实际 `watcherList` 语义调整 filter（实现时核对）。

- [ ] **Step 4: 跑确认通过 + 全套**

Run: `cd packages/core && npm test && npm run typecheck`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/store/front.ts packages/core/src/store/front.test.ts packages/core/src/present/tensionBoard.ts packages/core/src/present/tensionBoard.test.ts
git commit -m "feat(present): front_omen + tension_board 聚合视图

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec coverage（对 spec §4/§5/§7 的存储基座部分）：**
- 5 表（front/plotline/foreshadow/history/anchor）✓ Task 1（schema）+ 2/3（CRUD）。
- watcher.source ✓ Task 1。
- front_omen 视图 ✓ Task 4。tension_board 视图 ✓ Task 4。
- anchor 关系图谱正反查 ✓ Task 3。
- history 表 ✓ Task 3（is_moment 已在 log，ADR-0021）。
- **不在本 plan（Plan B / step③）**：谓词引擎扩展（spec §3）、业务生命周期工具（step③ 声明）、recall 语义召回。明确延后。

**Placeholder 扫描：** 无 TBD；plotline/foreshadow/history 的 CRUD 给了 front/anchor 的完整模板 + 明确字段差异（"照此结构写"指向同文件givens 的具体模板，非 vague 占位）。

**类型一致性：** `Front`/`Plotline`/`Foreshadow`/`History`/`Anchor` 接口在 Task 2/3 定义，Task 4 `tensionBoard` 一致引用；`WatcherRow.source`（Task 1）被 Task 4 `frontOmenList`/`tensionBoard` 用；`watcherSet` 的 `source?` 参数贯穿。

**已知核对项（实现时确认，非阻塞）：** `watcherList` 是否已只返回 armed/active（Task 4 注已标）；`openDb`/`initSchema` 的确切导出名（照 db.test.ts 现有用法）。
