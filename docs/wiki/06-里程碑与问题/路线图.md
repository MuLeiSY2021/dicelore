# 路线图

> **本页职责**：未来工作的**有序批次**——「先还哪个、再还哪个」。**AI 维护，可随时重排**（区别于人工维护、过去时的 [里程碑.md](里程碑.md)）。
> **怎么读**：每批列「修哪些 fix / 提哪些 feat」，每项链接回三个分层 backlog 池（[前端](backlog-前端.md) / [后端](backlog-后端.md) / [core](backlog-core.md)）的对应条目。批次顺序源自原问题账「排序总结（先还哪个）」+ P0–P5 优先级。

---

## 第一批 · meta 闸（最优先）

> **P1 真 harness 是 meta 闸**——不建则一切「行为类/措辞类」结论作废，继续跑 eval 只产污染数据。**下一步就做这个**，再跑剧本2/3 eval。

- feat：搭真 eval harness（子代理当 GM + mock 玩家程序，每动作经 `eval/tool.ts`/`batch.ts` 调真引擎）→ 见 [backlog-core 主题F · F1](backlog-core.md)。
- feat：终局「由谁敲、何时敲」纳入观测，harness 建好后才能测真 GM 收不收局 → 见 [backlog-core 主题F · F2](backlog-core.md)。

**闸的意义**：本批建好后，三池里所有 ⚠️（待真harness）项才可重新评定。

---

## 第二批 · 头号架构债（紧随）

> **P2 运行时叙事脚手架 ADR**——反复出现 + 随规模恶化（✓✓✓）= 全项目头号架构债。**先 A′（地基级 store 对齐）再 A（叙事脚手架建其上）**。需一个 ADR + 一个设计周期，勿往 gm-core 提示词硬塞。

- feat：A′ 数据层重构续做（叙事/记忆物理表 `front`/`plotline`/`foreshadow`/`history` 建表、视图层 `npc`/`player`/`world`/`relation`/`tension_board` 投影、声明式工具生成层、补充改名）→ 见 [backlog-core 主题A′](backlog-core.md)（🚧 step① 已落 main）。
- feat：A1–A5 运行时一等抽象（NPC/Front/plotline/foreshadow + 未结张力聚合视图），建在 A′ 之上 → 见 [backlog-core 主题A · A1–A5](backlog-core.md)。

---

## 第三批 · 在途实现线（按节奏）

> **P4 在途线**按节奏推（组件7 划算项 / 组件5/6 排期）。

- feat：组件7 后端 Play 会话生命周期端点（`POST start` / `DELETE` / `GET sessions`）→ 见 [backlog-后端 主题 · 组件7 后端](backlog-后端.md)（后端 spec 在写）。
- fix：`narration_commit.seq` 语义 / `GET /events` 重连补叙述 → 见 [backlog-后端 主题 · 组件7 后端](backlog-后端.md)。
- feat：团本构建 import 实现（组件5/6 四件套 + 包→四域映射）→ 见 [backlog-后端 主题 · 团本构建 · H-import](backlog-后端.md)；其中 H2-DEFER（叙事域构建工具）依赖第二批 A′ 叙事表落地。

---

## 第四批 · 增强 / 顺手（随设计周期带）

> **P3 增强 / P5 未来池**——随设计周期或顺手带。

- feat：裁决/披露/终局增强 B7（带修正∧分级检定）/ C1（分级线索披露）/ E1·E2（终局判据 + 机制）→ 见 [backlog-core 主题 · 裁决/披露/终局](backlog-core.md)；E1 待第一批 harness 能验证再动，E2 需 ADR。
- fix/feat：组件7 前端 fast-follow 中的可即修项（导航置灰、build 页 i18n）顺手清；其余（BG3 动效、面板拖拽、token 逐字、About 版本号、构建助手工具痕迹）随相关后端能力到位再带 → 见 [backlog-前端 主题 · 组件7 前端](backlog-前端.md)。

---

## 推迟池（不进当前批）

各池的 🔮 未来池项明确推迟，不编排进当前批次：[backlog-前端 未来池](backlog-前端.md) / [backlog-后端 未来池](backlog-后端.md) / [backlog-core 未来池](backlog-core.md)。
维护类（[backlog-core M1 · wiki 整理](backlog-core.md)）由 `organize-wiki` skill 持续处理，不占批次。

---

> [里程碑.md](里程碑.md) 人工维护、AI 不动（过去时、已达成节点）；**本页 AI 维护**（未来时、有序批次、可重排）。
