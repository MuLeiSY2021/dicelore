# 路线图

> **本页职责**：未来工作的**有序批次**——「先还哪个、再还哪个」。**AI 维护，可随时重排**（区别于人工维护、过去时的 [里程碑.md](里程碑.md)）。
> **怎么读**：每批列「修哪些 fix / 提哪些 feat」，每项链接回三个分层 backlog 池（[前端](backlog-前端.md) / [后端](backlog-后端.md) / [core](backlog-core.md)）的对应条目。批次顺序 = 优先级（反复出现 + 随规模恶化 = 靠前）+ 依赖链（前置闸先做），**AI 维护、可重排**。
>
> **2026-06-25 重排依据**（首席架构师评估）：项目最缺的不是架构能力，是「把一条链路端到端跑通并守住契约」的收敛力。故本版路线图以**头号债链路跑通**为唯一最高优先级，链路跑通前**冻结新 feat**；可观测性基建趁早（修复成本随代码指数增长）；安全 / 承重绑定单列**发版闸**。
>
> **2026-06-25 全量体检反指**（[汇总](../../audits/2026-06-25-全量体检/06-汇总-合并.md) / [findings](../../audits/2026-06-25-全量体检/findings.yaml)）：44 条去重 findings（10 P0 / 12 P1 / 14 P2 / 6 P3）已落 backlog 三池。其中"随规模恶化"类（CROSS-TIMEOUT/BE-002/CROSS-E2E/CROSS-BOUNDARY-TEST）靠前挂第三批横切基建同期；P0 新发现（USER-001 README CLI / BE-003 lore WS / QA-002·003 F1·F2 机械检测）编入对应批次；**8 个未决问题已于 2026-06-25 主流程裁决完成**（见 §✅ 已裁决），落 backlog 三池 + ADR-0017 补注 + 新 ADR-0026/0027 草案（待 PO 复核）。**既有承接条目加"体检实证"注记**确认实现状态。

---

## 第一批 · meta 闸（harness + 真 GM 接教条）— 基本达成，F2 收尾

> **真 GM 接教条 + 真 eval harness 是一条线**。harness 已建、真 GM 已接，本批只剩 F2 多轮跑测收局。**本批是后续一切「行为类」结论可信的前提**——不闭环则三池所有 ⚠️（待真harness）项继续作废。

- feat：真 GM 接 gm-core skill（`DiceGm` skillStage 真接、去 `openingPrompt` stopgap）+ `RUN_LIVE` 验证 → [backlog-后端 · G-后端-gmcore](backlog-后端.md)。**✅ 达成**。
- feat：搭真 eval harness（CC 经 play-mcp 连真后端当玩家+评估者，[ADR-0025](../05-决策记录-ADR/README.md)）→ [backlog-core 主题F · F1](backlog-core.md)。**✅ 达成**。
- feat：终局「由谁敲、何时敲」纳入观测 → [backlog-core 主题F · F2](backlog-core.md)。**🟡 harness 建好，待多轮跑测收局**。
- feat：**F1/F2 机械地板深化**（2026-06-25 体检 P0 新发现）→ [backlog-core · F1-floor / F2-floor](backlog-core.md)。F1"掷骰绕过率"无机械检测（GM 不骰纯 narrate 编结果不会被标记 fail）、F2 软着陆无机械地板 + baseline 对照报告缺失——eval 作为发版依据的根基风险。挂本批 F2 收尾深化或紧随：① `assertions` 加 F1 时序校验 + scenario.json `expects` 字段；② baseline 对照跑测（每场景 doctrine + baseline 各 N 局）；③ grader 对抗性 rubric + 人评抽查落地。
- fix：**eval harness 自测覆盖**（2026-06-25 体检 P2）→ [backlog-core · F-harness-test](backlog-core.md)。play-mcp.test.ts 补 choose/roll/browse/错误路径/list_scenarios/grade.ts 端到端。非阻塞但发版闸前应补。

> **达成（2026-06-24）**：G-后端-gmcore ✅ + F1 ✅ + F2 🟡。meta 闸基本达成——三池 ⚠️（待真harness）项可重新评定。F2 收尾与第二批 dogfooding 链路跑通**可并行**（harness 本就需真团本，dogfooding 产出的团本正好喂它）。F1/F2 机械地板深化是 F2 收尾的深化层——不补则 eval 报告可信度无保障。

---

## 第二批 · 头号债链路跑通（冻结令）⬅ 最高优先级

> **契约兑现批——不兑现一切都是沙上塔。** 头号架构债（[backlog-core 主题A/A′](backlog-core.md)）的存储地基已完、toolgen 引擎已建（981 行），但**零接线**：spec 承诺的「新剧本 = 新声明 + 新 flow、框架代码零改动」**目前跑不通**，团本扩展机制未对作者开放。这是「半截工程」——比没做更危险，因为它制造了「已解决」的错觉。
>
> **冻结令**：本批链路跑通前，**冻结一切新 feat**（第一批 F2 收尾除外）。一个跑通的真实 dogfooding 链路 > 十张设计页。
>
> **依赖链（单向，被旧路线图掩盖）**：叙事层总纲(定视图契约) → 谓词扩展(step①建表✅) → toolgen(step②引擎✅但视图契约未兑现) → 业务工具声明(step③❌) → dogfooding 团本端到端。**视图层是闸**。

- feat：**视图层投影落地**（`npc`/`player`/`world`/`relation`/`clock`/`tension_board` 命名视图）—— step②/③ 的**前置闸** → [backlog-core 主题A′ ①](backlog-core.md)。**✅ 达成（2026-06-25）**：`store/views.ts` `initViews` 用 `defineView` 投影 6 视图、接进 `initSchema`；`tension_board` UNION 四表统一 `kind/id/label/status` 列契约。core 405 测试 + tsc 全绿。前置闸已拆除，②/③ 可起。
- feat：**业务 MCP 工具暴露**（front/plotline/foreshadow/tension_board 走声明式 dogfooding，守 spec DT-9「团本扩展框架零改动」契约）→ [backlog-core 主题A′ ②](backlog-core.md)。**✅ 达成（2026-06-25）**：`mcp/stdlib/narration.ts` 八叙事工具全用声明（front_open/plotline_*·foreshadow_*·tension_board），零硬编码 handler。NPC 一等抽象（A1）另见下条（正交、未做）。
- feat：**toolgen 接线**（进 `createMcpServer` / dogfooding step③）→ [backlog-core 主题A′ ③](backlog-core.md)。**✅ 达成（2026-06-25）**：适配层 `toolgenToToolDef` + `createMcpServer(db,deps,extraTools?)` 接缝（DT-9 守约现有 19 工具零改动）+ dogfooding 集成测试（落库/承重墙不破/坏声明拒）。**团本侧 `tools:` 段 import 装载是独立后续**（依赖 manifest schema + 共享 validator）。
- feat：**NPC 一等抽象**（A1，state 的 npc kind 升一等表/工具）→ [backlog-core 主题A · A1](backlog-core.md)。🚧 未做——npc 视图已投影（①），但 npc 专属业务工具（npc_update 等类型化写）未声明；与团本侧 tools 装载同周期带。
- feat：**构建工具补全**（plotline/foreshadow/anchor 构建工具，随 dogfooding 带）→ [backlog-后端 · H-build-tools](backlog-后端.md)
- feat：**团本构建(lore)侧 eval harness 补建**——接构建工具补全后接本项；现 eval 只覆盖跑团 dice 侧，lore 侧零评估（构建流程「作者↔构建 GM」交互 / import 映射 / 构建工具可用性全无客观验证）→ [backlog-core 主题F · F3](backlog-core.md)。**已裁决（2026-06-25）：先补 lore 侧 eval**——mock 作者↔真构建 GM，或复用 play-mcp 模式经 build HTTP 驱动；与第二批 H-build-tools 同周期。终局场景 eval 随后补（依赖 [E2 ADR-0026](../05-决策记录-ADR/README.md) 草案落地）。**2026-06-25 体检实证（CROSS-LORE-EVAL P0）**：eval scenarios/ 仅 4 个全 dice 侧，lore 侧零场景文件——F3 是 F1 在 lore 侧的对称缺口。
- feat：**dogfooding 团本端到端跑通**（用一个真团本走「新声明 + 新 flow → 框架零改动」全链路，作为契约兑现的验收门）→ 验收 [主题A′ ③](backlog-core.md) + DT-9。**🟡 框架标准库侧已达成（2026-06-25）**：叙事层八工具全声明、经 server 端到端落库 + 承重墙不破 + 框架 core 零改动（dogfooding 集成测试证）。**仍欠真 LLM 完整一局**——喂给 F1 eval harness（真 GM 调声明叙事工具跑一局），与第一批 F2 收尾同周期；及**团本侧自定义 tools 装载**（独立后续）。
- feat/fix：**lore 路径 WS 流式**（BE-003 P0）→ [backlog-后端 · RT-5](backlog-后端.md)。**已裁决（2026-06-25）：v1 删死代码标 REST only（WS 推 v2）**。LoreSession 持 hub + 共享 streamDriverTurn 但 lore API 无 WS 端点——构建过程前端黑盒。v1 删 `LoreSession.hub`/`attachWs`/`detachWs` 死代码并 wiki 标注"lore 构建 v1 REST only、走轮询/等待"，WS 流式推 v2。

> **验收口径**：存在一个 dogfooding 团本，仅靠「声明 + flow」跑通完整一局（含 front/plotline/foreshadow/tension_board 的读），**框架 core 代码零改动**。跑不通则本批未竟，冻结令不撤。

---

## 第三批 · 可观测性基建 + 随规模恶化健壮性（趁早，独立于头号债）

> **为什么提前**：[backlog-core 主题O](backlog-core.md) 病根——全仓 7 处裸 `console.*`、后端运行时零日志、前端 0 日志。**修复成本随代码增长指数上升**，越晚抽 logger 越痛。本批**独立于头号债链路**（不碰叙事脚手架），可在第二批链路跑通后立即接，或与第二批并行由另一线推进。
>
> **不破坏冻结令**：logger 是横切基建、非业务 feat，且抽得越早收益越大，故单列不归入冻结范围。
>
> **2026-06-25 体检新增·随规模恶化靠前**：体检发现"随规模恶化"的后端健壮性 + 测试边界条目挂本批同期——本机单玩场景风险低，但 eval 多会话/并发/远程部署场景放大，越晚修回归风险越高（见 [体检汇总 §共性病根 6](../../audits/2026-06-25-全量体检/06-汇总-合并.md)"脱困不恢复 + 路径不对称"）。

- feat：抽**同构** `shared/logger`（core/后端/前端共用、`error/warn/info/debug` 分级 + sessionId/turnId 上下文，浏览器 + node 通用）→ [backlog-core O1](backlog-core.md)
- feat：core 运行时接入（`cli.ts` 面向终端输出**保留**、`mcp/main.ts` 裸 error 改走 logger）→ [backlog-core O2](backlog-core.md)
- feat：后端 HTTP/WS/会话生命周期/编排/错误分级覆盖 → [backlog-后端 O-后端](backlog-后端.md)
- feat：前端渲染/WS 重连/选项提交/错误分级覆盖（sink 走浏览器 console）→ [backlog-前端 O-前端](backlog-前端.md)
- feat：**token 用量采集**（orchestrator Agent SDK 回传处采 input/output/cache token、按 turn/session/agent 归因落库；归因维度 ADR 前置——澄清「每 mcp/skill」实为 per agent/工具调用链）→ [backlog-后端 · CO-后端-采集](backlog-后端.md)。横切基建、同 O 理由不破坏冻结令；是 [SEC2](backlog-后端.md) 计费/限流数据前置。**2026-06-25 体检实证（CROSS-TOKEN-RAW P3）**：raw log 已记 usage，本项是从 raw log 抽 usage → 结构化 schema → 归因落库，非"零采集"。
- feat：**GM 超时兜底"脱困不恢复"修复**（CROSS-TIMEOUT P1，随规模恶化）→ [backlog-后端 · RT-1](backlog-后端.md)。超时 abort 后已落库变更不回滚、已发 narration 不撤回、无重试——半途状态留库致游戏态渐进腐坏。短期：超时 error 显示给玩家 + 重试/跳过选项；中期：回合级事务（记 turn_start_seq，超时 DELETE FROM log WHERE seq > turn_start_seq）；长期：与快照接线合并。
- feat：**会话级并发互斥**（BE-002 P1，随规模恶化）→ [backlog-后端 · RT-2](backlog-后端.md)。同会话并发 handleMessage 无互斥——双击/重发/WS重连+REST 同时触发会双开回合，GM 上下文与 DB 写入竞态。DiceSession 加 in-flight Promise 队列串行化。
- feat：**e2e 玩家主线扩展**（CROSS-E2E P1，随规模恶化）→ [backlog-前端 · FE-e2e](backlog-前端.md)。现 e2e 只覆盖开局段，掷骰/选择/终局/错误恢复/断线重连五条玩家主线零端到端验证。扩 FAKE_GM 脚本 + 补五条 e2e + 单测补 useSession 边界。
- feat：**并发/超时/重启/注入四类边界测试**（CROSS-BOUNDARY-TEST P1，随规模恶化）→ [backlog-core · TB-1](backlog-core.md)。四类是"改一处坏别处"高回归风险区——无测试守则任何后端改动可能默默 break 而测试全绿。依赖 RT-1/RT-2/[RT-3](backlog-后端.md) 修复后补对应测试。
- feat：**live 测试 mock SDK 化**（CROSS-LIVE-TEST P1，随规模恶化）→ [backlog-core · TB-2](backlog-core.md)。DiceGm.live.test.ts 默认 skip，真 SDK 路径零回归保护——抽 SDK 装配逻辑为纯函数 FAKE_GM 模式下跑装配断言，或加 mock SDK 跑 DiceGm.runTurn 非 LLM 部分；真 LLM live 测试发版前必跑一次（纳入第五批发版闸 checklist）。与第五批 [主题S · S2](backlog-core.md) port 契约重构配套。
- docs：**README "CLI 一键开玩"文档对齐**（USER-001 P0，发版闸前必对齐）→ [backlog-前端 · FE-CLI-doc](backlog-前端.md)。README 承诺 CLI 一键开玩但 CLI 无 play 子命令——玩家按文档装完卡死。**已裁决（2026-06-25，见 [ADR-0027](../05-决策记录-ADR/README.md) 草案）**：不补 CLI play，CLI 退回开发/会话管理；走多端整合包发版架构（server docker-compose 自托管 + 客户端整合包安卓/Mac/Win/Linux + 安卓/Win 优先）。属发版闸前文档-实现一致性，本批或第四批带。

---

## 第四批 · 增强 / 顺手（随设计周期带）

> **P3 增强 / P5 未来池**——头号债链路跑通后、发版闸前，随设计周期或顺手带。

- feat：裁决/披露/终局增强 B7（带修正∧分级检定）/ C1（分级线索披露）/ E1·E2（终局判据 + 机制）→ [backlog-core 主题 · 裁决/披露/终局](backlog-core.md)；**E1/E2 已裁决（2026-06-25，见 [ADR-0026](../05-决策记录-ADR/README.md) 草案）**：GM 自调 game_end + 软判据 + 玩家主动结束 + 终局后三去向（结束/要 AI 建议/回溯续命 v2）；E1 教条措辞待 F2 harness 多轮数据校准。**2026-06-25 体检实证（CROSS-END P0）**：终局判据缺失是场景 A 验收口径不可达的根因之一。
- feat：**终局后玩家流程**（USER-002 P1）→ [backlog-前端 · FE-end-lock](backlog-前端.md)。终局后玩家锁死无重开入口——**已裁决（[ADR-0026](../05-决策记录-ADR/README.md) 草案）**：v1 去掉"回滚"按钮（[SNAP-1](backlog-core.md) 快照 v1 降预期不做手动回滚），只留"结束/重开新局/要 AI 建议"三按钮；回溯续命留 v2 依赖快照接线。与 E2 ADR 配套。
- feat：`mapCanonWrite` 前端解析 `changes` 增量（减全量对账，A2 优化面）→ [backlog-后端 · G-后端-mapkind](backlog-后端.md)
- fix：**前端错误吞没 + WS 重连竞态 + 缝B start 契约 + testModel res.ok + Markdown XSS 防回归 + 死代码 + 拖拽残留**（2026-06-25 体检 P1/P2/P3，顺手带）→ [backlog-前端 · FE-err-swallow / FE-ws-race / FE-start-contract / FE-testModel-ok / FE-md-xss / FE-dead-code / FE-drag-residue](backlog-前端.md)。这些是玩家主线健壮性的散点 fix，随相关后端能力到位或 clean up 时带。
- feat：**自定义 MCP 配置接入**（FE-012 P2）→ [backlog-前端 · FE-mcp-config](backlog-前端.md)。**已裁决（2026-06-25）：v1 不接入留 v2**——v1 配置页标注"仅本地、不参与 GM 运行时"；v2 规划：①agent 经 skill 扫 MCP 包安全性 ②服务端可配置是否接受自定义 MCP ③服务端可配置是否开 MCP 安全扫描（耗 token）④后端可单独刷新 session 更新列表（turnover 后刷新）。
- feat：**长对话前端护栏**（CROSS-LONG-CONTEXT P3）→ [backlog-前端 · FE-long-ctx](backlog-前端.md)。narration 数组加上限/虚拟列表 + Markdown memo + 输入 maxLength + "建议开新局"轻推。
- fix：**rollGate 重启死锁**（CROSS-GATE P2）→ [backlog-后端 · RT-3](backlog-后端.md)。handleRoll waiters 空时走"无 gate 立即掷"分支或 restagePendingRolls 重建 waiters。非阻塞但与 RT-1 超时修复同根。
- feat：**enrich 收口**（CROSS-ENRICH P2，非阻塞）→ [backlog-后端 · RT-6](backlog-后端.md)。发版闸前 port 契约定型时一并收口，关联 [主题S · S2](backlog-core.md)。
- fix/feat：组件7 前端 fast-follow 可即修项随相关后端能力到位带 → [backlog-前端 主题 · 组件7 前端](backlog-前端.md)
- feat：**token / 金钱消耗可视化界面**（per-turn token + 饼图按维度拆分 + 金钱换算）→ [backlog-前端 · CO-前端-可视化](backlog-前端.md)。依赖第三批 [CO-后端-采集](backlog-后端.md) + 归因 ADR 落地
- feat：**D 流畅派 v1 是否实现**（CROSS-D P2）→ [backlog-core · TB-4](backlog-core.md)。**已裁决（2026-06-25）：v1 不实现（标理论锚点）**——多智能体编排 v1 不实现；流畅派透明化（token 流式/thinking/skill·MCP 显示可关折叠防剧透/人性化行为描述）也是 v2（见 [backlog-前端 未来池「前端过程透明化」](backlog-前端.md)）。需 organize-wiki 在用户与场景 §1 标注"D 分型 v1 不实现、仅理论锚点"。

---

## 第五批 · 发版闸（安全 + 承重绑定 ADR + gate 机制）— 里程碑三前必做

> **发版前必须正面回答的三件事**（首席架构师评估）：承重绑定单点依赖、prompt injection 面、多租户隔离。这些不阻塞头号债链路，但**里程碑三发版前必决/必做**，否则不可上市。本批为发版闸，不进日常编排。
>
> **2026-06-25 体检实证（CROSS-GATE-RELEASE P0）**：发版闸名不副实——是"发版前提醒单"而非"发版闸"。四项发版闸全未实现且无 CI 检查/阻断脚本真拦住（grep gate|闸|enforce|block.*release across CI/config 无命中）。四项当前实现状态全 ✗（承重 port 契约 ✗ ARCH-004、prompt injection 威胁建模 ✗ ARCH-009、模型 key 托管 ✗ USER-007 key 明文存 localStorage、多租户隔离 ✗ ARCH-001 零鉴权）。**gate 机制必须补**——否则人忘了一次或赶进度跳过一次就形同虚设。

- feat：**承重绑定 ADR + 承重层 port 契约**（定解绑触发条件；把 hook/skill/subagent 承重层抽成 port——v1 单 Claude Code adapter、多 agent sdk 列未来，核心解 **L3 承重跨 agent 表达**（被动 rule 召回 + 回合末审计），正是 [ADR-0008](../05-决策记录-ADR/README.md) 否①的理由；禁 `adapter/` 外业务穿透；显式列为最高级架构风险。**不翻 ADR-0008**、是 [ADR-0017](../05-决策记录-ADR/README.md) 对冲延伸）→ [backlog-core 主题S · S1+S2](backlog-core.md)。**2026-06-25 体检实证（ARCH-004 P0）**：DiceGm 直接耦合 SDK query()、AgentFactory 是 factory 非 port——实现状态 ✗。
- feat：**威胁建模**（玩家输入→GM 的 prompt injection 面逐条评估结构防护）→ [backlog-后端 SEC1](backlog-后端.md)。**2026-06-25 体检实证（CROSS-INJECT P1）**：玩家自由文本直接作 prompt、validatePack 只验结构不验内容——实现状态 ✗。
- feat：**模型 key 托管 + 基础限流** → [backlog-后端 SEC2](backlog-后端.md)。**2026-06-25 体检实证（CROSS-KEY P1）**：key 明文存 localStorage + 后端代发 SSRF（model-test baseUrl 完全可控无白名单）——实现状态 ✗。**已裁决（2026-06-25）**：**统一后端托管**——key 都存后端（整合包存本地后端、自托管存远程后端），前端 localStorage 不存 key 只存引用；SSRF 白名单修（model-test baseUrl 限 https+host 白名单、mcp-test endpoint 同）。与 [ADR-0027](../05-决策记录-ADR/README.md) 草案多端整合包发版架构配套（整合包内含本地后端 sidecar 托管 key）。
- feat：**多租户模型 + SQLite 单文件隔离**（MT1 + SEC3 同根）→ [backlog-后端 MT1](backlog-后端.md) / [SEC3](backlog-后端.md)。**2026-06-25 体检实证（CROSS-AUTH P0）**：Hono app 零中间件、sessionId 即权限、DELETE 零鉴权不可逆、lore Catalog 可被任意客户端污染——实现状态 ✗。
- feat：**发版闸 gate 机制**（CROSS-GATE-RELEASE P0 新增）→ 路线图自指。① 加发版前 checklist 脚本（`scripts/release-gate-check.ts`）：扫鉴权中间件是否存在、SSRF 白名单是否配、key 是否走后端托管、port 契约 ADR 是否落地，任一缺失 exit 1；② CI 加发版闸标签门：release PR 必须带 `release-gate-passed` 标签（由 checklist 脚本生成），否则 block merge；③ backlog 三池相关条目加"冻结中/不破坏冻结令"标记与路线图双向对齐（见 §冻结论对齐）。
- fix：**GET /sessions/:id ended 硬编码**（BE-007-fix P1，独立 fix）→ [backlog-后端 · RT-4](backlog-后端.md)。读 `metaGet(db,"ended")` 填 ended 字段修 REST/WS 信号矛盾。不依赖 E2 ADR，可本批或更早带。
- feat：**快照/回滚 v1 开放范围**（CROSS-SNAP P1）→ [backlog-core · SNAP-1](backlog-core.md)。**已裁决（2026-06-25，见 [ADR-0017](../05-决策记录-ADR/README.md) v1 降预期补注）**：**v1 降预期**——v1 做自动持久化（存档/读档），手动回滚/branch 留 v2；终局续命/死错反悔/超时恢复（[RT-1](backlog-后端.md) 长期 restore）都留未来/v2。v1 实现步骤：① snapshot 表 rebase 进 initSchema；② core 补 checkpoint/restore + SnapshotParticipant 注册表；③ turnEnd 调 checkpoint()；④ 前端加存档/读档入口（自动恢复最近快照）。与 USER-002 终局后流程（第四批）配套——v1 去掉"回滚"按钮只留三按钮。

---

## ✅ 已裁决（2026-06-25 全量体检 8 项未决问题，主流程已落账）

> **8 项已由主流程裁决，落点见各条；新 ADR 草案待 PO 复核。**体检留 8 个未决问题（见 [体检汇总 §未决问题](../../audits/2026-06-25-全量体检/06-汇总-合并.md)），均属"v1 做 vs 不做"的产品/架构决策，AI 不代断。2026-06-25 主流程裁决完成，落点如下：

1. **ADR-0017 快照/回滚 v1 是否对玩家开放**（CROSS-SNAP P1）→ 第五批 [SNAP-1](backlog-core.md)。**裁决：v1 降预期**——v1 做自动持久化（存档/读档），手动回滚/branch 留 v2；终局续命/死错反悔/超时恢复都留未来/v2。落点：[backlog-core SNAP-1](backlog-core.md) + [ADR-0017](../05-决策记录-ADR/README.md) 补注 + [backlog-后端 RT-1](backlog-后端.md) 长期 restore 标 v2。
2. **终局机制"何时敲/由谁敲"**（CROSS-END P0）→ 第四批 [E1/E2](backlog-core.md)。**裁决**：GM 自调 game_end（gm-core 教条教何时收场）+ 软判据（不用 HP<=0 硬条件避免复活误判）+ 玩家主动结束权 + 终局后三去向（①结束 ②要 AI 建议 ③回溯续命 v2 依赖快照）。落点：[backlog-core E1/E2](backlog-core.md) + [backlog-前端 FE-end-lock](backlog-前端.md)（v1 去掉回滚按钮只留三按钮）+ **新 ADR-0026 草案·待 PO 复核**（`docs/wiki/05-决策记录-ADR/README.md`）。
3. **D 流畅派 v1 是否实现**（CROSS-D P2）→ 第四批 [TB-4](backlog-core.md)。**裁决：v1 不实现**（标理论锚点）；多智能体编排 v1 不实现；流畅派透明化也是 v2。落点：[backlog-core TB-4](backlog-core.md) + [backlog-前端 未来池「前端过程透明化」](backlog-前端.md) 新增 v2 条目 + [M1](backlog-core.md) 加注 organize-wiki 标 D 分型 v1 不实现。
4. **README "CLI 一键开玩"**（USER-001 P0）→ 第三批 [FE-CLI-doc](backlog-前端.md)。**裁决**：不补 CLI play，CLI 退回开发/会话管理；走多端发版架构（server docker-compose 自托管 + 客户端整合包安卓/Mac/Win/Linux + 安卓/Win 优先）。落点：[backlog-前端 FE-CLI-doc](backlog-前端.md) + **新 ADR-0027 草案·待 PO 复核**（`docs/wiki/05-决策记录-ADR/README.md`）+ [backlog-core M1-d](backlog-core.md) 加注移动端提前 + [backlog-前端 Tauri 壳/移动端](backlog-前端.md) 加注关联 ADR。
5. **lore 路径 WS 流式 v1 是否补**（BE-003 P0）→ 第二批 [RT-5](backlog-后端.md)。**裁决：v1 不补 lore WS，推 v2**；v1 删 `LoreSession.hub`/`attachWs`/`detachWs` 死代码，wiki 标注"lore v1 REST only、构建走轮询/等待"。落点：[backlog-后端 RT-5](backlog-后端.md) + [backlog-core TB-3](backlog-core.md) 标随 v2 WS 补。
6. **eval 场景覆盖度扩展顺序**（CROSS-LORE-EVAL P0）→ 第二批 [F3](backlog-core.md)。**裁决：先补 lore 侧 eval**（mock 作者↔真构建 GM 或复用 play-mcp 经 build HTTP 驱动），与第二批 H-build-tools 同周期；终局场景随后补（依赖 E2 ADR 落地）。落点：[backlog-core F3](backlog-core.md)。
7. **自定义 MCP 配置 v1 是否接入 GM 运行时**（FE-012 P2）→ 第四批 [FE-mcp-config](backlog-前端.md)。**裁决：v1 不接入留 v2**，配置页标注"仅本地、不参与 GM 运行时"；v2 规划四点（①agent 经 skill 扫 MCP 包安全性 ②服务端可配置是否接受 ③服务端可配置是否开安全扫描 ④后端可单独刷新 session 更新列表）。落点：[backlog-前端 FE-mcp-config](backlog-前端.md)。
8. **API key 存储方案**（CROSS-KEY P1）→ 第五批 [SEC2](backlog-后端.md)。**裁决：统一后端托管**——key 都存后端（整合包存本地后端、自托管存远程后端），前端 localStorage 不存 key 只存引用；SSRF 白名单修（model-test baseUrl 限 https+host 白名单、mcp-test endpoint 同）。落点：[backlog-后端 SEC2](backlog-后端.md) + 与 [ADR-0027](../05-决策记录-ADR/README.md) 草案多端整合包发版架构配套。

---

## 冻结论与 backlog 对齐（PROD-012-FROZEN，P3）

> **2026-06-25 体检实证**：第二批冻结令外仍有 feat 项在路线图第三/四批编排（标"不破坏冻结令"——横切基建/随设计周期带），但 backlog 三池里这些 feat 项（O1/CO-后端-采集/B7/C1/E1 等）本身没标"冻结中/不冻结"状态，上下游对冻结范围理解可能不一致。
>
> **对齐规则**：横切基建（O1/CO-后端-采集/RT-1/RT-2/TB-1/TB-2 等随规模恶化类）标"不破坏冻结令、可与第二批并行"；增强（B7/C1/E1/FE-end-lock/FE-long-ctx 等）标"冻结中、第四批解冻后带"。已在对应批次注明。backlog 三池相关条目由 `organize-wiki` 专项补"冻结中/不破坏冻结令"标记（属 [backlog-core M1](backlog-core.md) 范畴）。

---

## 推迟池（不进当前批）

各池的 🔮 未来池项明确推迟，不编排进当前批次：[backlog-前端 未来池](backlog-前端.md) / [backlog-后端 未来池](backlog-后端.md) / [backlog-core 未来池](backlog-core.md)。
维护类（[backlog-core M1 · wiki 整理](backlog-core.md)）由 `organize-wiki` skill 持续处理，不占批次。

---

## 已达成批（归档，供回溯）

- **原 wiring 收尾批（2026-06-24 达成）**：narration 来源 / game_end 补发 / POST /choices 去绕路 / seq 语义 / GET /events 重连 / packName 补填 / show 审计 event_id 回 AI（[backlog-后端](backlog-后端.md) G-后端-* 各项 ✅、[backlog-core V1](backlog-core.md) ✅）。单测 core 395 / orchestrator 75 / web 44 + 三包 tsc + FAKE_GM HTTP 三层绿。

---

> [里程碑.md](里程碑.md) 人工维护、AI 不动（过去时、已达成节点）；**本页 AI 维护**（未来时、有序批次、可重排）。
